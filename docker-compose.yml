version: '3.1'

services:
  pudelekfeed:
    build:
      context: ./pudelekfeed
    environment:
      ENVIRONMENT: DEV

  rabbitmq:
    build:
      context: ./rabbitmq
      dockerfile: Dockerfile
    ports:
      - "15672:15672"
      - "5672:5672"
      - "5671:5671"